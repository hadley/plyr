#' @details If there are no results, then this function will return a list of
#'   length 0 (\code{list()}).
#' @return if results are atomic with same type and dimensionality, a
#'   vector, matrix or array; otherwise, a list-array (a list with
#'   dimensions)
#' @family list output
